# NextJS w/ Docker Project Template
- NextJS + Typescript + Arrow Function + Tailwind + Docker

## Development Server

### Start

```bash
docker compose up -d
```

### Clean

```bash
docker compose down --volumes --rmi local
```
